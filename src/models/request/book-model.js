const bookModel = {
    id: String,
    name: String,
    year: Number,
    author: String,
    summary: String,
    publisher: String,
    pageCount: Number,
    readPage: Number,
    finished: Boolean,
    reading: Boolean,
    insertedAt: String,
    updatedAt: String
}

module.exports = bookModel;